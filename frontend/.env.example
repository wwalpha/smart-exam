# APIGW base URL (no trailing slash)
# Example: https://xxxxxxxxxx.execute-api.ap-northeast-1.amazonaws.com
VITE_APIGW_URL=

# Enable auth integration (1: enabled, 0/empty: disabled)
VITE_ENABLE_AUTH=0

# Cognito Managed Login (production only)
# Example domain: https://your-domain.auth.ap-northeast-1.amazoncognito.com
VITE_COGNITO_DOMAIN=
# Cognito User Pool App Client ID
VITE_COGNITO_CLIENT_ID=
# Redirect URI configured in Cognito app client (example: https://app.example.com/auth/callback)
VITE_COGNITO_REDIRECT_URI=

# MSW (mock API) is disabled by default
# Set to 1 only when you intentionally want mock API in local dev
VITE_USE_MSW=0
